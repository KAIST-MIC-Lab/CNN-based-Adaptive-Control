% ============================================
%         NEURAL NETWORKS
% ============================================
\newcommand*{\NN}{\mv{\Phi}}            % neural network
\newcommand*{\act}{\mv{\phi}}           % activation function
\newcommand*{\wth}{\mv{\theta}}         % weight (vectorized)
\newcommand*{\wV}{\mm{W}}               % weight matrix

\newcommand*{\estwth}{\widehat{\wth}}   % estimated weight (vectorized)
\newcommand*{\estNN}{\widehat{\NN}}     % NN with estimated weights
\newcommand*{\estact}{\widehat{\act}}   % activation function with estimated weights
\newcommand*{\estwV}{\widehat{\wV}}     % weight matrix

\newcommand*{\idealwth}{{\wth}^*}       % ideal weight (vectorized)
\newcommand*{\idealNN}{{\NN}^*}         % NN with ideal weights
\newcommand*{\idealact}{{\act}^*}       % activation function with ideal weights
\newcommand*{\idealwV}{{\wV}^*}         % ideal weight matrix